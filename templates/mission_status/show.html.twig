{% extends 'base.html.twig' %}

{% block title %}MissionStatus{% endblock %}

{% block body %}
    <h1>MissionStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mission_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mission_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mission_status_index') }}">back to list</a>

    <a href="{{ path('app_mission_status_edit', {'id': mission_status.id}) }}">edit</a>

    {{ include('mission_status/_delete_form.html.twig') }}
{% endblock %}
