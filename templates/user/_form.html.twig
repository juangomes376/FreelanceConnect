{{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
    <div class="space-y-4 font-mono">
        {{ form_widget(form) }}
    </div>
    <button type="submit" class="mt-6 w-full py-4 bg-[var(--color-drac-cyan)] text-[var(--color-drac-bg)] font-bold font-mono rounded-lg hover:bg-[var(--color-drac-green)] hover:shadow-[var(--color-drac-green)]/40 transition-all uppercase tracking-widest text-sm">
        {{ button_label|default('Executer_Commit') }}
    </button>
{{ form_end(form) }}
